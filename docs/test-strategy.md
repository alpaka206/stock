# Test Strategy

## Current Status
- ???? ?? ??? ???? `pnpm verify:standard`?.
- 2026-04-08 ?? ?? ?? ??? ?? ????.
- `scripts/api_smoke.py`? ?? 200 ??? ??? Pydantic response schema?? ????.

## Decisions
- unattended ?? ??? `pnpm verify:standard`? ???? ????.
- web-only ??? `pnpm verify:web`, API-only ??? `pnpm verify:api`? ????.
- schema drift? `python scripts/check_contract_parity.py`, route smoke? `python scripts/api_smoke.py`? ????.

## Latest Verification Snapshot
- `pnpm lint:web` -> pass
- `pnpm typecheck:web` -> pass
- `pnpm build:web` -> pass
- `python scripts/check_contract_parity.py` -> pass
- `python -m py_compile ...` -> pass
- `python scripts/api_smoke.py` -> pass (`/overview`, `/radar`, `/stocks/NVDA`, `/history`, `/news`, `/calendar`)
- `pnpm verify:standard` -> pass

## Coverage Today
- ?? ??: schema / prompt copy / Pydantic drift ??
- ??? ?? ??: `py_compile`
- ??? ??? ??: FastAPI mock smoke
- ????? ?? ??: lint + typecheck + build
- ?? ??? ??: build ?? ?? `/news`, `/calendar` route ?? ?? ??/?? route ??

## Remaining Risks
- secrets? ?? real provider smoke? ?? ????? ???.
- browser E2E? ?? interaction regression? ?? ? ???.
- provider-level unit test? ?? external API ?? ??? ????.
- ?? 4?? real path? OpenAI ??? ?? ???? ???? ???? ???.

## Next Work
- `respx` ?? ??? mocking ?? provider unit test? ????.
- Playwright smoke? ?? ??? ??? ???.
- secrets? ?? ???? real-provider smoke? ?? gated job?? ????.
- `/health`? readiness ??? ??? ? ?? smoke? ????.

## 2026-04-09 Release verification lane
- 표준 게이트:
  - `pnpm verify:standard`
  - mock/fixture 친화적 개발 회귀 검증
- 릴리스 게이트:
  - `pnpm verify:release`
  - real provider env 존재 확인
  - `/readyz?probe=remote` 확인
  - `/overview`, `/radar`, `/stocks/NVDA`, `/history`, `/news`, `/calendar` 실경로 smoke
- 현재 세션에서는 실 provider 비밀값이 없으면 릴리스 게이트를 녹색으로 만들 수 없다. 이 경우 구현/문서/표준 검증을 끝낸 뒤 외부 블로커로 보고한다.

## 2026-04-11 LLM Fallback Coverage
- ??? ??? `gemini_responses.py`, `summary_router.py`, `deterministic_summary.py`? ????.
- mock smoke? OpenAI ? ??? readiness? route schema? ?? ???? ??.
- real smoke? ?? OpenAI-only, Gemini-only, no-LLM fallback ? ?? ???? ?? ???? ?? ???.
