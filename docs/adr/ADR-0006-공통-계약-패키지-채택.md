# ADR-0006. 공통 계약 패키지 채택

## 상태

유지. 백엔드 분리 이후에도 `packages/contracts`가 화면 응답 계약의 기준이다.

## 결정

Next.js 프런트 타입과 Spring Boot 백엔드 응답은 `packages/contracts`의 TypeScript 타입과 JSON schema를 기준으로 맞춘다. 추후 LLM worker를 붙일 때도 같은 schema를 structured output 검증 기준으로 사용한다.
