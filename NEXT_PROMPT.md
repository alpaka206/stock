1. PR #132 의 CI와 auto-merge 결과를 확인한다.
2. 실제 배포 프런트 URL을 찾을 수 있으면 `/overview`, `/radar`, `/stocks/NVDA`, `/history` 를 다시 직접 확인한다.
3. `develop_loop -> develop` 승격이 필요한 시점인지 원격 PR 상태를 점검한다.
4. 배포 프런트 직접 확인이 어렵다면 필요한 외부 접근 범위를 한국어로 정리한다.
