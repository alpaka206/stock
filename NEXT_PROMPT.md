1. `issue/131-최상위-계약-primary-task` 브랜치 변경사항을 커밋하고 `develop_loop` 대상 PR을 만든다.
2. PR 설명에 원격 API cold start 타임아웃 복구 배경과 검증 결과를 한국어로 정리한다.
3. 실제 배포 프런트 URL을 찾을 수 있으면 `/overview`, `/radar`, `/stocks/NVDA`, `/history` 를 다시 직접 확인한다.
4. `develop_loop -> develop` 승격이 필요한 시점인지 원격 PR 상태를 점검한다.
