# Next Prompt

Next iteration order:
1. read `AGENTS.md`, `TASK.md`, `STATE.md`, `BACKLOG.md`, `DISCORD_STATUS.md`
2. run a short planner -> critic -> architect -> executor loop for risky changes
3. check `.env.discord` existence and key presence only, never print values
4. execute the top P0 item
5. update journal and state files
